# RaadAva ⚡
> It's logical.

این برنامه با هدف کمک به افرادی نوشته شده است که در انتخاب یوزرنیم تلگرام با مشکل روبه‌رو هستند. از آنجایی که بسیاری از یوزرنیم‌های ساده قبلاً رزرو شده‌اند، این برنامه به صورت خودکار یوزرنیم‌های تصادفی تولید می‌کند و وضعیت آن‌ها را بررسی می‌کند. همچنین هدف دیگر از انجام این پروژه، تمرین برنامه‌نویسی با زبان Python و آشنایی با ارسال درخواست‌های اینترنتی بوده است.
این پروژه به زبان پایتون نوشته شده و چون تلگرام API عمومی برای بررسی یوزرنیم ندارد، از روش https://t.me/username استفاده می‌کند؛ یعنی یوزرنیم ساخته‌شده را در این لینک قرار می‌دهیم و با ارسال درخواست HTTP وضعیت آن را بررسی می‌کنیم.
این برنامه به صورت خودکار یوزرنیم‌های ۵ حرفی را به شکل تصادفی تولید می‌کند. سپس برای بررسی هر یوزرنیم، یک درخواست به آدرس سایت تلگرام ارسال می‌شود و یوزرنیم ساخته‌شده به جای کلمه username در لینک قرار می‌گیرد. بعد از دریافت پاسخ از سرور تلگرام، برنامه بررسی می‌کند که آیا یوزرنیم آزاد است یا قبلاً گرفته شده است. اگر یوزرنیم آزاد باشد، نتیجه OK و اگر قبلاً استفاده شده باشد، نتیجه NO نمایش داده می‌شود. این روند چندین بار پشت سر هم تکرار می‌شود.
این سورس کد، یوزرنیم‌هایی که وضعیت آن‌ها OK باشد را با رنگ سبز در ترمینال نمایش می‌دهد و یوزرنیم‌هایی که NO باشند را با رنگ قرمز چاپ می‌کند تا کاربر بتواند راحت‌تر نتیجه‌ها را تشخیص دهد.
توجه مهم:
این سورس قابلیت بررسی اینکه این ایدی در سایت فرگمنت (Fragment) برای فروش گذاشته شده یا نه را ندارد.
همچنین ممکن است یوزرنیمی که صاحبش اکانت خود را حذف (Delete Account) کرده است را به اشتباه «آزاد» تشخیص دهد، در حالی که تلگرام تا مدتی این یوزرنیم را رزرو نگه می‌دارد و اجازه ثبت دوباره نمی‌دهد.
این پروژه از چند بخش اصلی تشکیل شده است. زبان اصلی برنامه Python است.
از کتابخانه requests برای ارسال درخواست‌های HTTP استفاده می‌شود
از کتابخانه random برای ساخت یوزرنیم‌های تصادفی
از کتابخانه json برای تحلیل پاسخ‌های سرور تلگرام
از کتابخانه time برای ایجاد تأخیر بین درخواست‌ها و جلوگیری از مشکل و بلاک شدن
از کتابخانه‌های os و sys برای ارتباط بهتر با سیستم عامل و مدیریت اجرای برنامه
همچنین تمام کدهای اصلی برنامه داخل فایل 2.py نوشته شده است و با اجرای این فایل، کل برنامه اجرا می‌شود.

---

## 🚀 نصب و اجرای سریع (همه‌چی با یه دستور)

```bash
git clone https://github.com/K-z89/RaadAva.git && cd RaadAva && python 2.py
