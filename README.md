
# RaadAva ⚡
> It's logical.

RaadAva یک پروژه‌ی مینیمال نوشته‌شده با **Python** است.  
فقط شامل یک اسکریپت اصلی (`2.py`) و این فایل README است؛ ساده، سبک و آماده‌ی اجرا.

---

## 🚀 نصب و اجرای فوری (کپی کن و اجرا کن)
بدون سوال، بدون دردسر — این تک‌خطو کپی کن و توی ترمینال بزنی:

```bash
git clone https://github.com/K-z89/RaadAva.git && cd RaadAva && pip install -r requirements.txt && python 2.py
```

تمام. اجرا شدی. خروجی رنگی آماده‌ست.

---

## 📝 درباره‌ی پروژه

این برنامه با هدف کمک به افرادی نوشته شده است که در انتخاب یوزرنیم تلگرام با مشکل روبه‌رو هستند. از آنجایی که بسیاری از یوزرنیم‌های ساده قبلاً رزرو شده‌اند، این برنامه به‌صورت خودکار یوزرنیم‌های تصادفی تولید می‌کند و وضعیت آن‌ها را بررسی می‌کند. همچنین هدف دیگر از انجام این پروژه، تمرین برنامه‌نویسی با زبان Python و آشنایی با ارسال درخواست‌های اینترنتی بوده است.

روش کار: برای هر یوزرنیم تولیدشده، یک درخواست HTTP به آدرس https://t.me/<username> ارسال می‌شود و بر اساس پاسخ سرور تشخیص داده می‌شود که یوزرنیم آزاد است یا نه. یوزرنیم‌های آزاد با `OK` (رنگ سبز) و یوزرنیم‌های گرفته‌شده با `NO` (رنگ قرمز) نمایش داده می‌شوند.

### توجه مهم
- امکان دارد یوزرنیم‌هایی که صاحبشان اکانت را حذف کرده‌اند به‌صورت موقت «آزاد» نمایش داده شوند، چون تلگرام ممکن است یوزرنیم را برای مدتی رزرو نگه دارد.
- این اسکریپت بررسی فروش در سایت‌هایی مثل Fragment را انجام نمی‌دهد.
- ارسال تعداد زیادی درخواست متوالی ممکن است منجر به محدودیت یا بلاک موقت شود — از تاخیر بین درخواست‌ها استفاده کن.

---

## ⚙️ ساختار و اجزای اصلی پروژه

- `2.py` — اسکریپت اصلی
- `README.md` — همین فایل
- `requirements.txt` — وابستگی‌ها

بخش‌های اصلی کد:
- تولید یوزرنیم‌های تصادفی (`random`)
- ارسال درخواست HTTP (`requests`)
- مدیریت تاخیر (`time`)
- تعامل با سیستم (`os`, `sys`)
- پردازش پاسخ‌ها (در صورت نیاز `json`)

---

## 📦 نیازمندی‌ها

- Python 3.8+
- بسته‌ها در `requirements.txt` (فعلاً فقط `requests`)

---

## ▶️ اجرا و استفاده

تک‌خط نصب و اجرا:
```bash
git clone https://github.com/K-z89/RaadAva.git && cd RaadAva && pip install -r requirements.txt && python 2.py
```

نتایج در ترمینال چاپ می‌شوند؛ یوزرنیم‌های آزاد سبز و یوزرنیم‌های پر قرمز نمایش داده می‌شوند.

---

## 🤝 مشارکت

اگه خواستی قابلیت اضافه کنیم: تنظیم طول یوزرنیم، نرخ درخواست، ذخیرهٔ OKها توی فایل، یا ارسال نوتیف، بگو تا اضافه کنم.

```
