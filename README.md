```markdown
# RaadAva ⚡
> It's logical.

RaadAva یک پروژه‌ی مینیمال نوشته‌شده با **Python** است.  
فقط شامل یک اسکریپت اصلی (`2.py`) و این فایل README است؛ ساده، سبک و آماده‌ی اجرا.

---

## 🚀 نصب و اجرای سریع

این بخش فقط برای اینه که کاربر **بدون فکر اضافه** بتونه پروژه رو اجرا کنه.  
کافیه این دستورها رو توی ترمینال کپی و اجرا کنی:

```bash
git clone https://github.com/K-z89/RaadAva.git && cd RaadAva
# نصب وابستگی‌ها از فایل requirements.txt:
pip install -r requirements.txt
# سپس اجرای برنامه:
python 2.py
```

---

## 📝 درباره‌ی پروژه

این برنامه با هدف کمک به افرادی نوشته شده است که در انتخاب یوزرنیم تلگرام با مشکل روبه‌رو هستند. از آنجایی که بسیاری از یوزرنیم‌های ساده قبلاً رزرو شده‌اند، این برنامه به صورت خودکار یوزرنیم‌های تصادفی تولید می‌کند و وضعیت آن‌ها را بررسی می‌کند. همچنین هدف دیگر از انجام این پروژه، تمرین برنامه‌نویسی با زبان Python و آشنایی با ارسال درخواست‌های اینترنتی بوده است.

این پروژه به زبان پایتون نوشته شده و چون تلگرام API عمومی برای بررسی یوزرنیم ندارد، از روش https://t.me/username استفاده می‌کند؛ یعنی یوزرنیم ساخته‌شده را در این لینک قرار می‌دهیم و با ارسال درخواست HTTP وضعیت آن را بررسی می‌کنیم.

این برنامه به صورت خودکار یوزرنیم‌های ۵ حرفی را به شکل تصادفی تولید می‌کند. سپس برای بررسی هر یوزرنیم، یک درخواست به آدرس سایت تلگرام ارسال می‌شود و یوزرنیم ساخته‌شده به جای کلمه `username` در لینک قرار می‌گیرد. بعد از دریافت پاسخ از سرور تلگرام، برنامه بررسی می‌کند که آیا یوزرنیم آزاد است یا قبلاً گرفته شده است. اگر یوزرنیم آزاد باشد، نتیجه `OK` و اگر قبلاً استفاده شده باشد، نتیجه `NO` نمایش داده می‌شود. این روند چندین بار پشت سر هم تکرار می‌شود.

سورس کد، یوزرنیم‌هایی که وضعیت آن‌ها `OK` باشد را با رنگ سبز در ترمینال نمایش می‌دهد و یوزرنیم‌هایی که `NO` باشند را با رنگ قرمز چاپ می‌کند تا کاربر بتواند راحت‌تر نتیجه‌ها را تشخیص دهد.

### توجه مهم
- این سورس قابلیت بررسی اینکه این آیدی در سایت فرگمنت (Fragment) برای فروش گذاشته شده یا نه را ندارد.
- ممکن است یوزرنیمی که صاحبش اکانت خود را حذف (Delete Account) کرده است را به اشتباه «آزاد» تشخیص دهد، در حالی که تلگرام تا مدتی این یوزرنیم را رزرو نگه می‌دارد و اجازه ثبت دوباره نمی‌دهد.

---

## ⚙️ ساختار و اجزای اصلی پروژه

- `2.py` — کد کامل و اجرای اصلی برنامه
- `README.md` — همین فایل
- `requirements.txt` — فهرست وابستگی‌ها (برای نصب سریع)

کد پروژه از چند بخش اصلی تشکیل شده است:
- تولید یوزرنیم‌های تصادفی (کتابخانه `random`)
- ارسال درخواست‌های HTTP برای بررسی وضعیت با استفاده از `requests`
- پردازش پاسخ‌ها (با `json` در صورت لزوم)
- اعمال تاخیر بین درخواست‌ها برای جلوگیری از بلاک شدن (کتابخانه `time`)
- استفاده از `os` و `sys` برای تعامل با سیستم عامل و مدیریت اجرا

---

## 📦 نیازمندی‌ها

- Python 3.8+
- بسته‌های پایتون در `requirements.txt`:
  - requests

نصب وابستگی‌ها:
```bash
pip install -r requirements.txt
```

---

## ▶️ اجرا و استفاده

برای اجرای ساده کافی است:
```bash
python 2.py
```

خروجی در ترمینال نمایش داده می‌شود؛ یوزرنیم‌های آزاد با رنگ سبز و یوزرنیم‌های اشغال‌شده با رنگ قرمز چاپ می‌شوند.

---

## 🛠 پیاده‌سازی و نکات فنی

- به‌دلیل نبود API رسمی تلگرام برای بررسی یوزرنیم، برنامه از بررسی صفحه‌ی https://t.me/<username> استفاده می‌کند که حالت محتوا/هدر پاسخ سرور نشان‌دهنده‌ی وضعیت است.
- بهتر است بین درخواست‌ها تاخیر مناسبی قرار داده شود تا از مواجهه با محدودیت یا بلاک شدن توسط تلگرام جلوگیری شود.
- در صورت تغییر ساختار پاسخ تلگرام یا مکانیزم‌های جدید، ممکن است لازم شود کد با تغییرات جدید هماهنگ شود.

---

## 🤝 مشارکت

اگر می‌خواهی دیگران هم مشارکت کنند می‌توانیم یک بخش CONTRIBUTING کوتاه اضافه کنیم. پیشنهاد رایج:
1. مخزن را fork کنید
2. یک branch جدید بسازید (feature/your-feature)
3. تغییرات خود را اضافه کنید و یک PR باز کنید
```